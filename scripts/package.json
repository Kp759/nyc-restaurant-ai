{
  "name": "@bitenyc/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "import:places": "tsx src/import-google-places.ts",
    "enrich:ai": "tsx src/enrich-ai.ts",
    "fetch:reels": "tsx src/fetch-reels.ts",
    "match:yelp": "tsx src/match-yelp.ts",
    "match:health": "tsx src/match-health.ts",
    "embeddings": "tsx src/generate-embeddings.ts",
    "create:admin": "tsx src/create-admin.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bitenyc/shared": "workspace:*",
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5",
    "openai": "^4.67.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
